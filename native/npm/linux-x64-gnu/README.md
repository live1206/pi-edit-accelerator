# @live1206/pi-edit-accelerator-linux-x64-gnu

Linux x64 glibc native backend for `@live1206/pi-edit-accelerator`.

This platform package is loaded optionally. Install the main package rather than depending on this package directly. If the binary is unavailable or cannot load, the main package uses its TypeScript implementation.
